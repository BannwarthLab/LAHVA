option(
  'lapack',
  type: 'combo',
  value: 'auto',
  yield: true,
  choices: ['auto', 'mkl', 'mkl-rt', 'openblas', 'netlib', 'custom'],
  description : 'linear algebra backend',
)

option(
  'custom_libraries',
  type: 'array',
  value: [],
  yield: true,
  description: 'libraries to load for custom linear algebra backend',
)

option(
  'gpu',
  type: 'feature',
  value: 'auto',
)

option(
  'gpu_arch', 
  type: 'array',
  value: ['native'],
  yield: true,
  description: 'GPU architecture version string',
)

option(
  'gpu_static',
  type: 'boolean',
  value: false,
  description: 'Link NVIDIA CUDA libraries statically (cudart_static, cublas_static, cusolver_static, cusparse_static) when available',
)
